<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="KidsMonitor_Tray.ChangeLimitWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KidsMonitor_Tray"
    Title="Change Daily Limit">
    <Grid Padding="24" RowSpacing="12" Width="360">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Change daily limit" FontSize="20" FontWeight="Bold" />

        <NumberBox x:Name="LimitBox" Grid.Row="1" Header="Daily time limit (minutes)" Value="120" Minimum="1" Maximum="1440" SpinButtonPlacementMode="Inline" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="2" Header="Current password" PlaceholderText="Enter current password" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="12">
            <Button x:Name="SubmitButton" Content="Save" Click="SubmitButton_Click" />
            <TextBlock x:Name="ErrorText" Foreground="Red" VerticalAlignment="Center" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Window>
